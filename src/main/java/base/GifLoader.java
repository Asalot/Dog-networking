package base;

import java.util.Date;
import java.util.List;

public class GifLoader extends TwitterLoader {

    public GifLoader(String instanceValue, Date dateValue, String patternValue, List<DogInfoPetharbor> filesValue) {
        super(instanceValue, dateValue, patternValue,filesValue);
    }

}